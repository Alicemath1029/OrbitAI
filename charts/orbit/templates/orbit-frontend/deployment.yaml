# nginx-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orbit-frontend
  namespace: {{ .Release.Namespace }}
  labels:
    app: orbit-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orbit-frontend
  template:
    metadata:
      labels:
        app: orbit-frontend
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/orbit-frontend/configmap.yaml") . | sha256sum }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
        - name: orbit-frontend-container
          image: {{ .Values.images.frontend.repository }}:{{ .Values.images.frontend.tag }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: 80
          volumeMounts:
            - name: config-volume
              mountPath: /usr/share/nginx/html/config.json
              subPath: config.json
      volumes:
        - name: config-volume
          configMap:
            name: orbit-frontend-config
      nodeSelector: {{ .Values.nodeSelector | toYaml | nindent 8 }}
      tolerations: {{ .Values.tolerations | toYaml | nindent 8 }}