apiVersion: v1
kind: Service
metadata:
  name: orbit-frontend-svc
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: orbit-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
