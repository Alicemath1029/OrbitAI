apiVersion: v1
kind: Service
metadata:
  name: orbit-backend-svc
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: orbit-backend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8088